collection:

  # AWS ECS ping
  - pattern: "ping"
    pipeline:

      - type: process
        cmd: echo pong

  # Route
  - pattern: "{*}"
    pipeline:

      - type: process
        cmd: python geoip.py
