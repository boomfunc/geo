collection:

  # AWS ECS ping
  - pattern: "{GET|HEAD} /ping HTTP/1.1"
    pipeline:

      - type: process
        cmd: |
          echo HTTP/1.1 204 No Content

  # Route
  - pattern: "{*}"
    pipeline:

      - type: process
        cmd: python geoip.py
