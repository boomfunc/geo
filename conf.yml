collection:

  # Route
  - pattern: "geo"
    pipeline:

      - type: process
        cmd: ./geo/geoip -lang=ru -ip=185.86.151

  # AWS ECS ping
  - pattern: "ping"
    pipeline:

      - type: process
        cmd: echo pong
